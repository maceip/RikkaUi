# Project exclude paths
/.gradle/
/.kotlin/

# Build outputs (all modules)
**/build/

# Generated registry JSON
composeApp/src/webMain/resources/r/
composeApp/src/webMain/resources/rikkaui.jar

buildSrc/.gradle/

RELEASE_NOTES.md

.claude/worktrees/

# Vendored JS deps for the gallery renderer
gallery/node_modules/

# Machine-local SDK path; must never be committed (it broke every Android build
# in this repo by pointing at a checkout that only existed on one laptop).
local.properties
